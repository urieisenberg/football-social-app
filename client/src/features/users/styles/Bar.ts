import styled from 'styled-components';

export const BarWrapper = styled.div.attrs({
  className: 'container text-center',
})``;

export const BarContainer = styled.div`
  display: block;
`;

export const BarLogo = styled.img.attrs({
  width: 40,
})``;

export const BarTitle = styled.h5``;

