import { useParams } from 'react-router-dom';
import { useGetFixtureLineupsQuery } from '../../api';
import { Transition } from '../../../../components/Transition';

export const FixtureLineups = () => {
  return <div>FixtureLineups</div>;
};
